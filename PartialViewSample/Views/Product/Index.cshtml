@model IEnumerable<Product>
@{
	ViewData["Title"] = "Product Index";
}

@Html.Partial("ProductList");

@* <h1 class="bg-success text-white">Product List --- Home/Index</h1>
<table class="table table-bordered table-hover">
    <thead>
        <tr>
            <th>Product ID</th>
            <th>Product Name</th>
            <th>Category Name</th>
            <th>Description</th>
            <th>Price</th>
        </tr>
    </thead>
    <tbody>
        @foreach (Product item in Model)
        {
            <tr>
                <td>@item.ProductID</td>
                <td>@item.ProductName</td>
                <td>@item.CategoryName</td>
                <td>@item.Description</td>
                <td>@item.Price</td>
            </tr>
        }
    </tbody>
    <tfoot class="text-center fs-4">
        <tr>
            <td colspan="5">@Model.Count() Items</td>
        </tr>
    </tfoot>
</table> *@